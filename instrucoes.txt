Para rodar o frontend:
npx build:frontend
npx webpack -w
(deve aparecer 'successfully' e o webpack vai "olhar" o código)

Para rodar o backend na pasta dist (compilar):
npx build:backend
npx tsc

os dois comandos devem ser executados na pasta raiz